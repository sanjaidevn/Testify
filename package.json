{
  "name": "copy-paste-website",
  "version": "1.0.0",
  "description": "Simple copy paste website with Netlify functions and Netlify Blobs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@netlify/blobs": "^7.0.0"
  }
}
